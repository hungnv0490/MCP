

public enum PoolType
{
    None,
    Box,
    PixcelItem,
    GameBox,
    Ball
}

public enum GameState
{
    WaitingToStart,
    Playing,
    Paused,
    Win,
    Fail
}

public enum BallColorType
{
    Red,
    Blue,
    Green,
    Yellow,
    Purple
}